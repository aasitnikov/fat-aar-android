include ':lib-main'
include ':lib-java'
include ':lib-aar'
include ':lib-aar2'
include ':lib-aar-local'
include ':app'

// The plugin is consumed as a single prebuilt binary (published once to mavenLocal)
// so the same artifact is tested against every AGP/Gradle combination.
// To rebuild against local plugin source: cd ../source && ./gradlew publishToMavenLocal
